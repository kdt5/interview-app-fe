export interface RankingItem {
  //이게 찐임
  nickname: string;
  rank?: number;
  level: number;
  profileImageUrl: string;
  totalFavoriteCount: number;
  totalAnswerCount: number;
  totalScore: number;
}
